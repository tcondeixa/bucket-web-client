<!DOCTYPE html>
<link rel="icon"
      type="image/png"
      href="/img/favicon.ico" />
<html>
  <head>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <img src="{{ .Picture }}" alt="Profile Google Picture" width="64" height="64">
    <a href="/logout">logout</a>
    <h1>Welcome to S3 Web {{ .Email }}.</h1>
    <ul>
        {{ range .S3Objects }}
        <li><a href="/download?object={{ . }}" target="_blank">{{ . }}</a></li>
        {{ end }}
    </ul>
  </body>
</html>